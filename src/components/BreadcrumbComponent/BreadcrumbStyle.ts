import { css } from "lit-element";

export default css`
  :host {
  }

  a {
    color: var(--primary-color);
    text-decoration: none;
  }

  .primary {
    color: var(--primary-color);
  }
`;

{
  "rules": {
    ".read": true,
    ".write": true,
    "users": {
      "$user_id": {
        ".read": "auth != null && auth.uid == $user_id",
        ".write": "$user_id === auth.uid"
      }
    }
  }
}
